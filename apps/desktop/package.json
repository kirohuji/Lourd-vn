{
  "name": "desktop",
  "version": "0.1.0",
  "description": "Tauri desktop app for NQTR game",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web build:h5 && tauri dev",
    "build": "pnpm --filter web build:h5 && tauri build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}